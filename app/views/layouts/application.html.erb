<!DOCTYPE html>
 <html class="no-js"> 
    <head>
      <meta charset="utf-8">
      <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
      <meta name="robots" content="noindex">
      <meta name="googlebot" content="noindex">
      <title><%= @configurations.store_name.blank? ? "Validsol Point of Sale" : @configurations.store_name %></title>
      <meta name="description" content="<%= @configurations.store_description.blank? ? "Validsol Point of Sale" : @configurations.store_description %>">
      <meta name="viewport" content ="width=device-width; initial-scale=1.0;">

      <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>

		  <%= csrf_meta_tags %>
    </head>
    
    <body>
      <div class="overlay">
        <div id="loading-img"></div>
      </div>
      <div class="modal-container">
          <%#=render  'template/modal'%>
      </div>
    	<% if user_signed_in? %>
    		<%= render :partial => 'template/menu' %>
    	<% end %>


    	<div class="pos_wrapper">
  
        <% if user_signed_in? %>
          <%= render :partial => 'template/breadcrumbs' %>
        <% end %>

				<%= yield %>

        <% if user_signed_in? %>
          <%= render :partial => 'template/footer' %>
        <% end %>
			</div>

  

      <%= javascript_include_tag :application, "data-turbolinks-track" => true %>


      <%= yield :javascript %>
      <div id="alert_info">
          <%= render :partial => 'template/alerts' %>  
      </div>

    </body>
</html>
