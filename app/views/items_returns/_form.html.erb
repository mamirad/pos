<%= form_with(model: items_return, local: true) do |form| %>
  <% if items_return.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(items_return.errors.count, "error") %> prohibited this items_return from being saved:</h2>

      <ul>
      <% items_return.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :return_at %>
    <%= form.date_select :return_at %>
  </div>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.text_field :item_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
